# Dukascopy Example

Example crawler that streams Dukascopy Forex market data into Temporis via the data source ingest API.

## Setup and run

```
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
python dukascopy.py
```
